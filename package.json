{
  "name": "autopurge-extension",
  "version": "1.0.0",
  "description": "Chrome extension that automatically purges browsing history for specified domains",
  "main": "extension/background.js",
  "scripts": {
    "lint": "eslint extension/**/*.js --config .eslintrc.js",
    "lint:fix": "eslint extension/**/*.js --config .eslintrc.js --fix",
    "zip": "mkdir -p dist && cd extension && zip -r ../dist/autopurge.zip . -x \"*.DS_Store\" \"*.git*\" && cd ..",
    "build": "npm run lint && npm run zip",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "chrome-extension",
    "privacy",
    "history-cleaner",
    "browsing-privacy"
  ],
  "author": "AutoPurge Team",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

